name: telegram_emoji_converter
description: Telegram video emoji WEBM VP9 converter
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.0.0
  path_provider: ^2.1.3
  share_plus: ^9.0.0
  ffmpeg_kit_flutter_full_gpl: 5.1.LTS

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
